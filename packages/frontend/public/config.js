// Application runtime config served statically.
// Edit values here for local development if needed.
window.appConfig = {
  // Backend base URL (runtime config). Reverted to 8080.
  apiBase: 'http://localhost:8080',
};
