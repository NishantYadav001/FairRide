{
  "name": "fair_ride",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/frontend",
    "start:backend": "npm run start --workspace=packages/backend"
  }
}